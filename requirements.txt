# DNSsifter Python dependencies
# Install with:  pip install -r requirements.txt
requests>=2.28
dnspython>=2.3
geoip2>=4.6
tqdm>=4.64
colorama>=0.4
# cdri_score.py uses only the Python 3 standard library (no extra deps).
